# Starship Zsh Plugin

This repository hosts a [Zsh](https://www.zsh.org) plugin that initializes [Starship](https://starship.rs). It runs `eval "$(starship init zsh)"` and does nothing else. You could and frankly should just put that in your `.zshrc`, but for various reasons I prefer the initialization process to be done by a plugin.
